/** Paths under `public/brand/`. Masthead renders them via CSS masks (see `@/components/layout/site-masthead` + `.wr-brand-*` in `globals.css`). */
export const BRAND_MARK_SRC = "/brand/rat.svg";
export const BRAND_LOGO_SRC = "/brand/logo.svg";
