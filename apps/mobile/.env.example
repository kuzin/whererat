# Expo reads env from apps/mobile/, not repo root (.env.local is for Next only).
# See ../../ENVIRONMENT.md

# Production API origin (no trailing slash). Omit = default https://whererat.com
# Example against local Next: EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:3000
EXPO_PUBLIC_API_BASE_URL=
